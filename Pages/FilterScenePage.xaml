<Page x:Class="WPF_CheckBox.Pages.FilterScenePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPF_CheckBox.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="数据筛选">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- 筛选面板 -->
        <Border Grid.Column="0" Background="#f8f8f8" BorderBrush="#ddd" BorderThickness="0,0,1,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="15">
                    <TextBlock Text="筛选条件" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                    
                    <Expander Header="商品类别" IsExpanded="True" Margin="0,0,0,10">
                        <StackPanel Margin="15,10,0,0">
                            <CheckBox Content="电子产品" x:Name="electronicsCategory" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                            <CheckBox Content="家居用品" x:Name="homeCategory" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                            <CheckBox Content="厨房用具" x:Name="kitchenCategory" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                            <CheckBox Content="办公用品" x:Name="officeCategory" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                            <CheckBox Content="户外装备" x:Name="outdoorCategory" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                        </StackPanel>
                    </Expander>
                    
                    <Expander Header="价格区间" IsExpanded="True" Margin="0,0,0,10">
                        <StackPanel Margin="15,10,0,0">
                            <CheckBox Content="¥0 - ¥100" x:Name="price0to100" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                            <CheckBox Content="¥100 - ¥500" x:Name="price100to500" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                            <CheckBox Content="¥500 - ¥1000" x:Name="price500to1000" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                            <CheckBox Content="¥1000以上" x:Name="priceOver1000" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                        </StackPanel>
                    </Expander>
                    
                    <Expander Header="用户评分" IsExpanded="True" Margin="0,0,0,10">
                        <StackPanel Margin="15,10,0,0">
                            <CheckBox Content="5星" x:Name="rating5" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                            <CheckBox Content="4星" x:Name="rating4" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                            <CheckBox Content="3星" x:Name="rating3" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                            <CheckBox Content="2星以下" x:Name="ratingBelow2" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                        </StackPanel>
                    </Expander>
                    
                    <Expander Header="其他选项" IsExpanded="True" Margin="0,0,0,10">
                        <StackPanel Margin="15,10,0,0">
                            <CheckBox Content="有货" x:Name="inStock" IsChecked="True" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                            <CheckBox Content="促销商品" x:Name="onSale" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                            <CheckBox Content="免运费" x:Name="freeShipping" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                        </StackPanel>
                    </Expander>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                        <Button Content="重置筛选" Padding="10,5" Margin="0,0,10,0" Click="ResetFilter_Click"/>
                        <Button Content="应用筛选" Background="#2196F3" Foreground="White" Padding="10,5" Click="ApplyFilter_Click"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>
        
        <!-- 产品列表 -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- 顶部操作栏 -->
            <Border Grid.Row="0" Background="White" BorderBrush="#ddd" BorderThickness="0,0,0,1" Padding="20,15">
                <Grid>
                    <TextBlock x:Name="resultCount" Text="显示0个结果" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="排序方式：" VerticalAlignment="Center"/>
                        <ComboBox x:Name="sortingComboBox" Width="120" SelectedIndex="0" Margin="5,0,0,0" SelectionChanged="SortingComboBox_SelectionChanged">
                            <ComboBoxItem Content="默认排序"/>
                            <ComboBoxItem Content="价格从低到高"/>
                            <ComboBoxItem Content="价格从高到低"/>
                            <ComboBoxItem Content="评分从高到低"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- 产品网格 -->
            <ScrollViewer Grid.Row="1" Padding="10">
                <WrapPanel x:Name="productsPanel">
                    <!-- 产品项在后台代码中动态添加 -->
                </WrapPanel>
            </ScrollViewer>
            
            <!-- 无结果提示 -->
            <TextBlock Grid.Row="1" 
                       x:Name="noResultsText" 
                       Text="没有符合筛选条件的商品" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       FontSize="16"
                       Foreground="#666"
                       Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Page> 